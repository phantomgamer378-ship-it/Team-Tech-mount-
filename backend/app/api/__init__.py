"""
API routers:
  - health.py    — GET /api/health (Phase 1)
  - analyze.py   — POST /api/analyze/audio (Phase 8)
  - liveness.py  — POST /api/liveness/{start,verify} (Phase 8/§9)
  - websocket.py — WS /ws/session/{id} real-time streaming (Phase 9)
  - webrtc.py    — WS /ws/webrtc/signal/{id} signaling (Phase H)
"""
