# Phase 1 — keep this list MINIMAL on purpose (§28: no unexplained deps).
# Heavyweight AI dependencies (torch, transformers, librosa, faster-whisper)
# get added in Phases 2–4 when the code that needs them lands.

fastapi>=0.110
uvicorn[standard]>=0.29
python-dotenv>=1.0
pydantic>=2.6

# --- Phase 2: audio preprocessing (§14) ---
numpy>=1.26
soundfile>=0.12.2   # WAV/FLAC/OGG/MP3 read+write, bundles libsndfile
librosa>=0.10.2     # resampling (soxr engine)

# --- Phase 3: AASIST-L voice/deepfake detector (§5) ---
torch>=2.2          # CPU wheels are fine on macOS arm64; no torchaudio needed yet

# --- dev / test ---
pytest>=8.0
httpx>=0.27  # required by fastapi.testclient
